# Static portfolio

Plain HTML, CSS, and vanilla JS. No build required.

**Run locally:**
```bash
npx serve .
```
Then open http://localhost:3000 (or the port shown).

**Deploy:** Upload the contents of this folder to any static host (e.g. GitHub Pages, Netlify, Vercel static).
